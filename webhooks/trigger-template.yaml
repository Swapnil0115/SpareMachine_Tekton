apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: pipeline-trigger-template
  namespace: tekton-pipelines
spec:
  params:
    - name: gitrevision
    - name: gitrepositoryurl
    - name: sha
    - name: action
    - name: pull_request_id
    - name: branch
    - name: API_PATH_PREFIX
    - name: REPO_FULL_NAME
    - name: TARGET_URL
    - name: DESCRIPTION
    - name: CONTEXT
    - name: STATE
    - name: AUTH_TYPE
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        generateName: push-to-github-$(uid)
        namespace: tekton-pipelines
      spec:
        serviceAccountName: pipeline
        pipelineRef:
          name: push-to-github
        params:
          - name: url
            value: $(tt.params.gitrepositoryurl)
          - name: revision
            value: $(tt.params.gitrevision)
          - name: sha
            value: $(tt.params.sha)
          - name: action
            value: "$(tt.params.action)"
          - name: pull_request_id
            value: "$(tt.params.pull_request_id)"
          - name: branch
            value: "$(tt.params.branch)"
          - name: REPO_FULL_NAME
            value: $(body.repository.full_name)
          - name: TARGET_URL
            value: https://github.com/$(body.repository.full_name)/commit/$(body.after)
          - name: DESCRIPTION
            value: "Build has started"
          - name: CONTEXT
            value: "continuous-integration/tekton"
          - name: STATE
            value: "pending"
          - name: AUTH_TYPE
            value: "Bearer"
          - name: API_PATH_PREFIX
            value: ""
        workspaces:
          - name: git-credentials
            secret:
              secretName: github-secret
          - name: tekton-learning-workspace
            volumeClaimTemplate:
              spec:
                accessModes:
                  - ReadWriteOnce
                resources:
                  requests:
                    storage: 10Gi
